--used for initializing and updating the sqlite database containing our markers.

--CREATE TABLE markers(id INTEGER PRIMARY KEY AUTOINCREMENT, x REAL NOT NULL, y REAL NOT NULL, name TEXT NOT NULL, installer INTEGER NOT NULL);

--INSERT INTO markers(x,y,name, installer)
--VALUES (12.103539004571969, -86.26922537500955, 
--"Censolar Nicaragua", 1);

SELECT * FROM markers;
